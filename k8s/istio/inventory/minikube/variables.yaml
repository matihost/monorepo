all:
  hosts:
    localhost:
  vars:
    operator:
      hub: "gcr.io/istio-release"
      tag: "1.11.4"
    ingress_class:
      enable: true
      default: false
    ingress_gateway:
      default_dns_suffix: internal.testing.minikube
    egress_gateway:
      enable: true
